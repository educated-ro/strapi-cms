'use strict';

/**
 * assessment-session router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::assessment-session.assessment-session');
