'use strict';

/**
 * assessment-session service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::assessment-session.assessment-session');
